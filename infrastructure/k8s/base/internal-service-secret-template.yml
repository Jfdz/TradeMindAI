# internal-service-secret-template.yml
#
# DO NOT commit real values. Populate the secret with:
#
#   kubectl create secret generic internal-service-secret \
#     --from-literal=market-data=$(openssl rand -base64 32) \
#     -n trading-saas

apiVersion: v1
kind: Secret
metadata:
  name: internal-service-secret
  namespace: trading-saas
type: Opaque
data:
  market-data: UkVQTEFDRV9NRQ== # REPLACE_ME - openssl rand -base64 32
