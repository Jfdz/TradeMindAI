name: CI — web-app

on:
  push:
    branches: [develop]
    paths:
      - 'services/web-app/**'
      - '.github/workflows/ci-web-app.yml'
      - '.github/workflows/ci-web-app-reusable.yml'
  pull_request:
    branches: [main, develop]
    paths:
      - 'services/web-app/**'
      - '.github/workflows/ci-web-app.yml'
      - '.github/workflows/ci-web-app-reusable.yml'
  workflow_dispatch:
    inputs:
      ref:
        description: Ref/SHA to build (defaults to triggering ref)
        required: false
        type: string

jobs:
  ci:
    name: CI
    permissions:
      contents: read
      packages: write
      actions: read
      security-events: write
    secrets: inherit
    uses: ./.github/workflows/ci-web-app-reusable.yml
    with:
      ref: ${{ github.event.inputs.ref || github.sha }}
      push_image: ${{ github.event_name == 'push' || github.event_name == 'workflow_dispatch' }}
