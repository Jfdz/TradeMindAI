name: CI — trading-core-service

on:
  push:
    branches: [develop]
    paths:
      - 'services/trading-core-service/**'
      - '.github/workflows/ci-trading-core-service.yml'
      - '.github/workflows/ci-trading-core-service-reusable.yml'
  pull_request:
    branches: [main, develop]
    paths:
      - 'services/trading-core-service/**'
      - '.github/workflows/ci-trading-core-service.yml'
      - '.github/workflows/ci-trading-core-service-reusable.yml'
  workflow_dispatch:
    inputs:
      ref:
        description: Ref/SHA to build (defaults to triggering ref)
        required: false
        type: string

jobs:
  ci:
    name: CI
    permissions:
      contents: read
      packages: write
      actions: read
      security-events: write
    secrets: inherit
    uses: ./.github/workflows/ci-trading-core-service-reusable.yml
    with:
      ref: ${{ github.event.inputs.ref || github.sha }}
      push_image: ${{ github.event_name == 'push' || github.event_name == 'workflow_dispatch' }}
