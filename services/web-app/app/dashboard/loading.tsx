import { RouteLoading } from "@/components/route-loading";

export default function Loading() {
  return <RouteLoading eyebrow="Dashboard" title="Loading dashboard" description="Fetching portfolio, signals, and chart data." />;
}
