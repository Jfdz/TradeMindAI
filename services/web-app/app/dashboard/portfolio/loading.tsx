import { RouteLoading } from "@/components/route-loading";

export default function Loading() {
  return <RouteLoading eyebrow="Portfolio" title="Loading portfolio" description="Fetching holdings, allocations, and P&L metrics." />;
}
