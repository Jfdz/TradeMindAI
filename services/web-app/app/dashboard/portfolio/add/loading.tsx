import { RouteLoading } from "@/components/route-loading";

export default function Loading() {
  return <RouteLoading eyebrow="Add Position" title="Loading symbol universe" description="Preparing the position form and available tickers." />;
}
