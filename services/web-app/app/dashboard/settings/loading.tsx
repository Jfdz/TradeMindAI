import { RouteLoading } from "@/components/route-loading";

export default function Loading() {
  return <RouteLoading eyebrow="Settings" title="Loading settings" description="Fetching profile and notification preferences." />;
}
