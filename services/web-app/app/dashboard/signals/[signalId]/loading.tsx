import { RouteLoading } from "@/components/route-loading";

export default function Loading() {
  return <RouteLoading eyebrow="Signal Detail" title="Loading signal detail" description="Fetching price action, confidence, and rationale." />;
}
