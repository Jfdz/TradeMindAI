import { RouteLoading } from "@/components/route-loading";

export default function Loading() {
  return <RouteLoading eyebrow="Signals" title="Loading signals" description="Pulling the latest AI signal feed and market context." />;
}
