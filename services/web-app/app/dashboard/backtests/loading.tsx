import { RouteLoading } from "@/components/route-loading";

export default function Loading() {
  return <RouteLoading eyebrow="Backtests" title="Loading backtests" description="Preparing the backtest workspace and recent run data." />;
}
